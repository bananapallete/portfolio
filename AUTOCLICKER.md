# 아이폰 미러링 자동 클릭 (macOS)

맥북에 미러링된 아이폰 화면 위를, 정해둔 순서대로 자동으로 눌러주는 스크립트입니다.
게임의 반복 퀘스트(예: **가방에서 상자 1개 사용하기**)를 대신 클릭해 줍니다.

- 미리보기(어디를 어떤 순서로 누르는지): `macro.html`
- 실제 자동 클릭: `auto_clicker.py`

---

## 좌표는 고정 안 해도 돼요 — "창"만 고정하면 됩니다

버튼 위치를 화면 픽셀로 박아두지 않고, **아이폰 화면 크기 대비 비율**로 저장해 둡니다.
그래서 미러링 창이 화면 어디에 있든, **처음 한 번 "보정"**만 하면 됩니다.

- ✅ 창을 그대로 두면 → 그냥 실행
- 🔄 창을 옮기거나 크기를 바꿨으면 → **1) 화면 위치 맞추기**만 다시

---

## 처음 준비 (한 번만)

1. 아이폰을 맥북에 **미러링** (macOS 기본 "iPhone 미러링" 앱 등)
2. 라이브러리 설치:
   ```bash
   pip3 install pyautogui pyobjc
   ```
3. 마우스 제어 권한 허용:
   **시스템 설정 → 개인정보 보호 및 보안 → 손쉬운 사용(접근성)** 에서
   **터미널**(또는 실행에 쓰는 앱)을 켜기

---

## 사용법

```bash
python3 auto_clicker.py
```

1. **1) 화면 위치 맞추기** 선택
   - 마우스를 미러링 화면의 **왼쪽 위** 모서리에 올리고 Enter
   - 마우스를 미러링 화면의 **오른쪽 아래** 모서리에 올리고 Enter
   - (맨 위 얇은 제목 표시줄 말고, **게임 화면 안쪽** 모서리 기준)
2. **2) 퀘스트 실행** 선택 → 퀘스트 번호, 반복 횟수, 간격 입력
3. 3초 카운트다운 뒤 자동으로 클릭 시작

### 멈추기
- **ESC 키** ← 실행 중 아무 때나 누르면 즉시 멈춤 (게임 화면을 보고 있어도 됨)
- 예비: 터미널에서 **Control + C**
- 예비: 마우스를 화면 **맨 왼쪽 위 모서리**로 휙 던지기 (안전장치)

> ESC가 안 먹히면 **시스템 설정 → 개인정보 보호 및 보안 → 입력 모니터링**에서 터미널을 켜보세요.
> 그래도 안 되면 Control + C 로 멈추면 됩니다.

---

## 새 퀘스트 추가하기

`auto_clicker.py` 의 `QUESTS` 에 위치(0~1 비율)만 추가하면 됩니다.

```python
QUESTS = {
    "2": {
        "name": "몬스터 저지하기",
        "steps": [
            {"label": "저지하기 버튼 누르기", "x": 0.83, "y": 0.63},
        ],
    },
}
```

비율(x, y)은 **아이폰 화면 기준**으로 `0.0`(왼쪽/위) ~ `1.0`(오른쪽/아래) 입니다.

---

> ⚠️ 게임 자동화는 게임사 정책 위반이 될 수 있습니다. 개인적으로, 본인 책임 하에 사용하세요.
